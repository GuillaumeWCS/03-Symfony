
{% extends 'base.html.twig' %}
{% block title %} Season {% endblock %}

{% block body %}

    <h1> Saison : {{ season.id }}</h1>
    <h4> Année : {{ season.year }} </h4>
    <h4> Description : {{ season.description }} </h4>

    {% for episode in season.episodes %}
        <a href="{{ path("show_episode", {"id":episode.number}) }}"> Episode : {{ episode.number }}</a>
        <p>{{ episode.title }}</p>
        <p>{{ episode.synopsis }}</p>
    {% endfor %}
{% endblock %}



